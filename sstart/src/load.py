"""Transactional SQL Server staging and upsert."""

def load_outputs(outputs, config, batch_id):
    # TODO: stage, upsert by order_item_id, audit and roll back on failure.
    raise NotImplementedError
